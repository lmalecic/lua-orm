return {
	Int = require("orm.types.int"),
	Decimal = require("orm.types.decimal"),
	Float = require("orm.types.float"),
	Varchar = require("orm.types.varchar"),
	Char = require("orm.types.char"),
	Text = require("orm.types.text"),
	Timestamp = require("orm.types.timestamp"),
	TimestampTz = require("orm.types.timestamptz"),
}

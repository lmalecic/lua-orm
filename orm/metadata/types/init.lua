return {
	Int = require("orm.metadata.types.int"),
	Decimal = require("orm.metadata.types.decimal"),
	Float = require("orm.metadata.types.float"),
	Varchar = require("orm.metadata.types.varchar"),
	Char = require("orm.metadata.types.char"),
	Text = require("orm.metadata.types.text"),
	Timestamp = require("orm.metadata.types.timestamp"),
	TimestampTz = require("orm.metadata.types.timestamptz"),
}
